Chapter no.2
2.1 message="hello python"
 print(message)
 hello python
 
2.2 message="ankita"
 print(message)
ankita
 new_message="ankita patil"
 print(new_message)
ankita patil

2.3 name="Eric python"
print("Hi!" + name.title() + ". this is python book")
 

2.4 msg=("Hello eric,would u like to learn python today")
 print(msg.upper())
HELLO ERIC,WOULD U LIKE TO LEARN PYTHON TODAY
msg=("ANKITA Patil")
print(msg.lower)

2.5 print('\tAndrew Kix promotes the idea that,"The secret about consistency is in turning the things\n\tyou hate doing, into things you do without thinking!"')

2.6 print('\t'+famous_person.title() + ' promotes the idea that,"The secret about consistency is in turning the things\n\tyou hate doing, into things you do without thinking!"')

2.7 favourite_language="python"
 favourite_language=favourite_language.rstrip()
 favourite_language
'python'

2.8 print(7+9)
print(18-13)
print(8*4)
print(25/5)

2.9 fav_no = 18
print(" my fav no is:" ")

chapter no.3

3.1 name=['ankita','patil']
print(name[0])
print(name[1])

name=['ankita','patil']
for names in name:
    print(names)

3.2 names = ["Alice", "Bob", "Charlie"]
for name in names:
print(f"Hello {name}, I hope you're having a great day!")

3.3 vehicles = ["Tesla car", "Ducati motorcycle", "Yamaha scooter"]
for vehicle in vehicles:
    print(f"I would like to own a {vehicle}.")

3.4 guests = ["Sachin Tendulkar", "Dr. A.P.J. Abdul Kalam", "Oprah Winfrey"]
for guest in guests:
    print(f"Dear {guest}, I would be honored to have you join me for dinner.")


3.5 guest_cant_make_it = "Dr. A.P.J. Abdul Kalam"
print(f"\nUnfortunately, {guest_cant_make_it} can't make it to the dinner.")

new_guest = "Elon Musk"
guests[guests.index(guest_cant_make_it)] = new_guest

print("\nNew Invitation List:")
for guest in guests:
    print(f"Dear {guest}, I would be honored to have you join me for dinner.")

3.6 print("\nGreat news! I found a bigger dinner table, so I am inviting more guests.")

guests.insert(0, "Bill Gates")  # Adding at the beginning
guests.insert(len(guests) // 2, "Cristiano Ronaldo")  # Adding in the middle
guests.append("Mother Teresa")  # Adding at the end

print("\nUpdated Invitation List:")
for guest in guests:
print(f"Dear {guest}, I would be honored to have you join me for dinner.")

3.7 guests = ["Bill Gates", "Sachin Tendulkar", "Cristiano Ronaldo", "Elon Musk", "Oprah Winfrey", "Mother Teresa"
print("\nUnfortunately, the new dinner table won't arrive in time, so I can invite only two people.")

while len(guests) > 2:
    removed_guest = guests.pop()
    print(f"Sorry {removed_guest}, I can't invite you to dinner.")

print("\nFinal Invitations:")
for guest in guests:
    print(f"Dear {guest}, you are still invited to dinner.")

del guests[:]
print("\nFinal guest list:", guests)  

3.8 cities = ["london", "munich", "cancun", "paris", "tokyo"]
print(cities)
print(sorted(cities))
print(cities)
print(sorted(cities, reverse=True))
print(cities)
cities.reverse()
print(cities)
cities.reverse()
print(cities)
cities.sort()
print(cities)
cities.sort(reverse = True)
print(cities)

3.9 invitations = ["Michael Jackson", "Jet Li", "Donnie Yen"]
count_invitations = len(invitations)

print("{} people will come to my dinner party".format(count_invitations))

Chapter no.4

pizzas = ["Pepperoni", "Margherita", "BBQ Chicken"]
for pizza in pizzas:
print(f"I like {pizza} pizza.")
print("\nI really love pizza!")
animals = ["Dog", "Cat", "Rabbit"]

4.2 for animal in animals:
print(f"A {animal} would make a great pet.")
print("\nAny of these animals would make a great pet!")

4.3 for number in range(1, 21):
    print(number)

4-4. One Million
# Creating a list of numbers from 1 to 1,000,000
numbers = list(range(1, 1000001))


4-5. Summing a Million

# Creating a list of numbers from 1 to 1,000,000
numbers = list(range(1, 1000001))

# Finding the minimum, maximum, and sum of the list
print("Minimum:", min(numbers))
print("Maximum:", max(numbers))
print("Sum:", sum(numbers))


4-6. Odd Numbers
for odd in range(1, 21, 2):
    print(odd)

4-7. Threes (Multiples of 3 from 3 to 30)
for multiple in range(3, 31, 3):
    print(multiple)

4-8. Cubes (First 10 Cubes)
for num in range(1, 11):
    print(num ** 3)

4-9.cubes = [num ** 3 for num in range(1, 11)]
print(cubes)

4-10.pizzas = ['margherita', 'pepperoni', 'hawaiian', 'veggie', 'bbq chicken', 'cheese']

print("The first three items in the list are:")
print(pizzas[:3])  # First three items

print("\nThree items from the middle of the list are:")
middle_index = len(pizzas) // 2
print(pizzas[middle_index-1:middle_index+2])  # Middle three items

print("\nThe last three items in the list are:")
print(pizzas[-3:])  # Last three items

4-11.pizzas = ['margherita', 'pepperoni', 'hawaiian']
friend_pizzas = pizzas[:]  # Make a copy

pizzas.append('cheese burst')         # Add to original list
friend_pizzas.append('spicy paneer')  # Add to friend’s list

print("\nMy favorite pizzas are:")
for pizza in pizzas:
    print(pizza)

print("\nMy friend's favorite pizzas are:")
for pizza in friend_pizzas:
    print(pizza)

4-12.my_foods = ['pizza', 'biryani', 'pasta']
friend_foods = ['burger', 'sushi', 'tacos']

print("My favorite foods are:")
for food in my_foods:
    print(food)

print("\nMy friend's favorite foods are:")
for food in friend_foods:
    print(food)

Chapter No.5
5-1.car = 'subaru'
print("Is car == 'subaru'? I predict True.")
print(car == 'subaru')  # True

print("\nIs car == 'audi'? I predict False.")
print(car == 'audi')  # False

animal = 'dog'
print("\nIs animal == 'dog'? I predict True.")
print(animal == 'dog')  # True

print("\nIs animal == 'cat'? I predict False.")
print(animal == 'cat')  # False

number = 42
print("\nIs number == 42? I predict True.")
print(number == 42)  # True

print("\nIs number != 42? I predict False.")
print(number != 42)  # False

print("\nIs number > 40? I predict True.")
print(number > 40)  # True

print("\nIs number < 40? I predict False.")
print(number < 40)  # False

print("\nIs number >= 42? I predict True.")
print(number >= 42)  # True

print("\nIs number <= 41? I predict False.")
print(number <= 41)  # False

5-2. More Conditional Tests

# Equality and inequality with strings
fruit = 'Apple'
print("\nIs fruit == 'Apple'? I predict True.")
print(fruit == 'Apple')  # True

print("Is fruit != 'Orange'? I predict True.")
print(fruit != 'Orange')  # True

print("Is fruit == 'apple'? I predict False.")
print(fruit == 'apple')  # False

# Using lower()
print("\nIs fruit.lower() == 'apple'? I predict True.")
print(fruit.lower() == 'apple')  # True

print("Is fruit.lower() != 'apple'? I predict False.")
print(fruit.lower() != 'apple')  # False

# Numerical tests
age = 21
print("\nIs age == 21? I predict True.")
print(age == 21)  # True

print("Is age != 30? I predict True.")
print(age != 30)  # True

print("Is age > 18? I predict True.")
print(age > 18)  # True

print("Is age < 18? I predict False.")
print(age < 18)  # False

print("Is age >= 21? I predict True.")
print(age >= 21)  # True

print("Is age <= 20? I predict False.")
print(age <= 20)  # False

# and/or
height = 180
weight = 70
print("\nIs height > 170 and weight < 80? I predict True.")
print(height > 170 and weight < 80)  # True

print("Is height > 190 or weight < 60? I predict False.")
print(height > 190 or weight < 60)  # False

# In list
colors = ['red', 'blue', 'green']
print("\nIs 'red' in colors? I predict True.")
print('red' in colors)  # True

print("Is 'yellow' in colors? I predict False.")
print('yellow' in colors)  # False

# Not in list
print("\nIs 'purple' not in colors? I predict True.")
print('purple' not in colors)  # True

print("Is 'blue' not in colors? I predict False.")
print('blue' not in colors)  # False

5-3.alien_color = 'green'
if alien_color == 'green':
    print("You just earned 5 points!")

# Version that fails the test (no output)
alien_color = 'red'

if alien_color == 'green':
    print("You just earned 5 points!")

5-4.alien_color = 'green'
if alien_color == 'green':
    print("You just earned 5 points for shooting the alien!")
else:
    print("You just earned 10 points!")

# Version that runs the else block
alien_color = 'yellow'

if alien_color == 'green':
    print("You just earned 5 points for shooting the alien!")
else:
    print("You just earned 10 points!")

5-5.alien_color = 'green'

if alien_color == 'green':
    print("You earned 5 points.")
elif alien_color == 'yellow':
    print("You earned 10 points.")
else:
    print("You earned 15 points.")

# Version where alien is yellow
alien_color = 'yellow'

if alien_color == 'green':
    print("You earned 5 points.")
elif alien_color == 'yellow':
    print("You earned 10 points.")
else:
    print("You earned 15 points.")

# Version where alien is red
alien_color = 'red'

if alien_color == 'green':
    print("You earned 5 points.")
elif alien_color == 'yellow':
    print("You earned 10 points.")
else:
    print("You earned 15 points.")

5-6.age = 25

if age < 2:
    print("The person is a baby.")
elif age < 4:
    print("The person is a toddler.")
elif age < 13:
    print("The person is a kid.")
elif age < 20:
    print("The person is a teenager.")
elif age < 65:
    print("The person is an adult.")
else:
    print("The person is an elder.")

5-7.favorite_fruits = ['mango', 'bananas', 'apple']

if 'mango' in favorite_fruits:
    print("You really like mangoes!")

if 'banana' in favorite_fruits:
    print("You really like bananas!")

if 'apple' in favorite_fruits:
    print("You really like apples!")

if 'orange' in favorite_fruits:
    print("You really like oranges!")  

if 'grape' in favorite_fruits:
    print("You really like grapes!") 

5-8.usernames = ['admin', 'eric', 'teja', 'rita', 'chaya']
for user in usernames:
    if user == 'admin':
        print("Hello admin, would you like to see a status report?")
    else:
        print(f"Hello {user.title()}, thank you for logging in again.")

5-9.usernames = []
if usernames:
    for user in usernames:
        if user == 'admin':
            print("Hello admin, would you like to see a status report?")
        else:
            print(f"Hello {user.title()}, thank you for logging in again.")
else:
    print("We need to find some users!")

5-10.current_users = ['rita', 'teja', 'Mina', 'Eric', 'Sarah']
new_users = ['sara', 'Bobby', 'john', 'lily', 'Mark']

# Make all current usernames lowercase for case-insensitive comparison
current_users_lower = [user.lower() for user in current_users]

for new_user in new_users:
    if new_user.lower() in current_users_lower:
        print(f"Sorry, the username '{new_user}' is already taken. Please enter a new one.")
    else:
        print(f"Username '{new_user}' is available.")

5-11.numbers = list(range(1, 10))
for num in numbers:
    if num == 1:
        print("1st")
    elif num == 2:
        print("2nd")
    elif num == 3:
        print("3rd")
    else:
        print(f"{num}th")

5-12.if(user=='admin'):
print("Hello admin,would you like to see a status report?")
else:print("Hello "+user+", thank you for logging in again.")

if user == 'admin':
    print("Hello admin, would you like to see a status report?")
else:
    print(f"Hello {user.title()}, thank you for logging in again.")

Chapter No.6
6-1.person = {
    'first_name': 'Amit',
    'last_name': 'Sharma',
    'age': 30,
    'city': 'Delhi'
}

print(f"First name: {person['first_name']}")
print(f"Last name: {person['last_name']}")
print(f"Age: {person['age']}")
print(f"City: {person['city']}")

6-2.favorite_numbers = {
    'Riya': 7,
    'Aman': 3,
    'Neha': 10,
    'Rahul': 5,
    'Priya': 9
}

for name, number in favorite_numbers.items():
    print(f"{name}'s favorite number is {number}.")

6-3.glossary = {
    'variable': 'A name that holds a value.',
    'list': 'An ordered collection of items.',
    'dictionary': 'A collection of key-value pairs.',
    'loop': 'A structure for repeating actions.',
    'function': 'A block of reusable code that performs a task.'
}

for word, meaning in glossary.items():
    print(f"{word}:\n  {meaning}\n")

6-4.glossary = {
    'variable': 'A name that holds a value.',
    'list': 'An ordered collection of items.',
    'dictionary': 'A collection of key-value pairs.',
    'loop': 'A structure for repeating actions.',
    'function': 'A block of reusable code that performs a task.',
    'boolean': 'A data type with two possible values: True or False.',
    'string': 'A sequence of characters enclosed in quotes.',
    'tuple': 'An immutable list.',
    'input': 'A function to take user input.',
    'comment': 'Text in code ignored by the interpreter, used for explanation.'
}

for word, meaning in glossary.items():
    print(f"{word}:\n  {meaning}\n")

6-5.rivers = {
    'nile': 'egypt',
    'amazon': 'brazil',
    'yangtze': 'china'
}

# Sentence about each river
for river, country in rivers.items():
    print(f"The {river.title()} runs through {country.title()}.")

print("\nRivers included in the dictionary:")
for river in rivers.keys():
    print(f"- {river.title()}")

print("\nCountries included in the dictionary:")
for country in rivers.values():
    print(f"- {country.title()}")

6-6.favorite_languages = {
    'alice': 'python',
    'bobby': 'java',
    'charlie': 'c++',
    'soniya': 'python'
}

people_to_poll = ['alice', 'eric', 'charlie', 'soniya', 'bobby']

for person in people_to_poll:
    if person in favorite_languages:
        print(f"Thank you, {person.title()}, for taking the poll.")
    else:
        print(f"{person.title()}, please take the favorite programming language poll!")

6-7.person1 = {'first_name': 'Amit', 'last_name': 'Sharma', 'age': 30, 'city': 'Delhi'}
person2 = {'first_name': 'Neha', 'last_name': 'Verma', 'age': 25, 'city': 'Mumbai'}
person3 = {'first_name': 'Ravi', 'last_name': 'Kumar', 'age': 35, 'city': 'Bangalore'}

people = [person1, person2, person3]

for person in people:
    print(f"\nPerson Details:")
    for key, value in person.items():
        print(f"{key.title()}: {value}")

6-8.pet1 = {'animal': 'dog', 'owner': 'Riya'}
pet2 = {'animal': 'cat', 'owner': 'Ayan'}
pet3 = {'animal': 'parrot', 'owner': 'Piya'}

pets = [pet1, pet2, pet3]

for pet in pets:
    print(f"\nPet Info:")
    for key, value in pet.items():
        print(f"{key.title()}: {value}")

6-9.favorite_places = {
    'Riya': ['Paris', 'Tokyo', 'Goa'],
    'Ayan': ['London', 'New York'],
    'Neha': ['Manali']
}

for name, places in favorite_places.items():
    print(f"\n{name}'s favorite places:")
    for place in places:
        print(f"- {place}")

6-10.favorite_numbers = {
    'Riya': [7, 14],
    'Aman': [3, 6],
    'Neha': [10],
    'Rahul': [5, 9],
    'Priya': [9, 11]
}

for name, numbers in favorite_numbers.items():
    print(f"\n{name}'s favorite numbers are:")
    for number in numbers:
        print(f"- {number}")

6-11.cities = {
    'delhi': {
        'country': 'India',
        'population': '30 million',
        'fact': 'It’s the capital of India.'
    },
    'paris': {
        'country': 'France',
        'population': '11 million',
        'fact': 'It is known as the City of Light.'
    },
    'tokyo': {
        'country': 'Japan',
        'population': '37 million',
        'fact': 'It’s the most populous city in the world.'
    }
}

for city, info in cities.items():
    print(f"\nCity: {city.title()}")
    for key, value in info.items():
        print(f"{key.title()}: {value}")
6-12.
cities = {
    'delhi': {
        'country': 'India',
        'population': '30 million',
        'fact': 'It’s the capital of India.',
        'famous_food': 'Chole Bhature',
        'is_capital': True
    },
    'paris': {
        'country': 'France',
        'population': '11 million',
        'fact': 'It is known as the City of Light.',
        'famous_food': 'Croissant',
        'is_capital': True
    },
    'tokyo': {
        'country': 'Japan',
        'population': '37 million',
        'fact': 'It’s the most populous city in the world.',
        'famous_food': 'Sushi',
        'is_capital': True
    }
}

for city, info in cities.items():
    print(f"\nCity: {city.title()}")
    for key, value in info.items():
        print(f"{key.replace('_', ' ').title()}: {value}")C

Chapter No.7
7-1.car = input("What kind of rental car would you like? ")
print(f"Let me see if I can find you a {car.title()}.")

7-2. Restaurant Seating
people = int(input("How many people are in your dinner group? "))
if people > 8:
    print("You'll have to wait for a table.")
else:
    print("Your table is ready.")

7-3. Multiples of Ten
number = int(input("Enter a number: "))
if number % 10 == 0:
    print(f"{number} is a multiple of 10.")
else:
    print(f"{number} is not a multiple of 10.")

7-4.print("Enter 'quit' when you are done adding toppings.")

while True:
    topping = input("Add a topping: ")
    if topping.lower() == 'quit':
        break
    print(f"I'll add {topping} to your pizza!")

7-5.print("Enter 'quit' when you are done.")

while True:
    age_input = input("What is your age? ")
    if age_input.lower() == 'quit':
        break
age = int(age_input)

    if age < 3:
        print("Your ticket is free.")
    elif age <= 12:
        print("Your ticket is $10.")
    else:
        print("Your ticket is $15.")

7-6.topping = ""
while topping != 'quit':
    topping = input("Enter a pizza topping : ")
    if topping != 'quit':
        print(f"Adding {topping} to your pizza.")
(2) Using an active variable
active = True

while active:
    age_input = input("Enter your age : ")
    if age_input.lower() == 'quit':
        active = False
    else:
        age = int(age_input)
        if age < 3:
            print("Ticket: Free")
        elif age <= 12:
            print("Ticket: $10")
        else:
            print("Ticket: $15")
(3) Using break
while True:
    topping = input("Add a topping (type 'quit' to stop): ")
    if topping.lower() == 'quit':
        break
    print(f"{topping} will be added to your pizza!")

7-7.while True:
    print("This loop will run forever! Press Ctrl+C to stop it.")

7-8.sandwich_orders = ['tuna', 'chicken', 'egg', 'cheese', 'grilled veggie']
finished_sandwiches = []

while sandwich_orders:
    current_sandwich = sandwich_orders.pop(0)
    print(f"I made your {current_sandwich} sandwich.")
    finished_sandwiches.append(current_sandwich)

print("\nAll sandwiches made:")
for sandwich in finished_sandwiches:
    print(f"- {sandwich} sandwich")

7-9.sandwich_orders = ['pastrami', 'tuna', 'pastrami', 'egg', 'pastrami', 'cheese']
finished_sandwiches = []

print("Sorry, the deli has run out of pastrami!\n")

# Remove all 'pastrami' sandwiches
while 'pastrami' in sandwich_orders:
    sandwich_orders.remove('pastrami')

while sandwich_orders:
    current_sandwich = sandwich_orders.pop(0)
    print(f"I made your {current_sandwich} sandwich.")
    finished_sandwiches.append(current_sandwich)

print("\nAll sandwiches made (excluding pastrami):")
for sandwich in finished_sandwiches:
    print(f"- {sandwich} sandwich")

7-10: Dream Vacation Poll
responses = {}

polling_active = True

while polling_active:
    name = input("What's your name? ")
    place = input("If you could visit one place in the world, where would you go? ")
    responses[name] = place

    repeat = input("Would you like to let another person respond? (yes/no): ")
    if repeat.lower() != 'yes':
        polling_active = False

print("\n--- Dream Vacation Poll Results ---")
for name, place in responses.items():
    print(f"{name.title()} would like to visit {place.title()}.")

Chapter No.8
8-1.def display_message():
    print("python is a easy language!")

display_message()

8-2.def favorite_book(title):
    print(f"One of my favorite books is {title}.")

favorite_book(""Harry Potter and the Sorcerer's Stone")

8-3.def make_shirt(size, message):
    print(f"The shirt is size {size} and has the message: '{message}'.")

make_shirt("Large", "I love Python")

8-4.def make_shirt(size='Large', message="I love Python"):
    print(f"The shirt is size {size} and has the message: '{message}'.")

make_shirt()  
make_shirt("Medium", "Python Rocks!")  

8-5.def describe_city(city, country):
    print(f"{city} is in {country}.")
describe_city("New York", "USA")

8-6. def describe_city(city,country="unknown")
       print(f"{city} is in {country}.")
describe_city("New York","USA")
describe_city("paris")

8-7.def make_album(singer, title):
    return {"singer": singer, "title": title}

# Creating an album dictionary for Arijit Singh
album = make_album("Arijit Singh", "tera yaar hoon main")
print(album)

8-8. def make_album(artist, title, tracks=None):
    album = {"artist": artist, "title": title}
    if tracks:
        album["tracks"] = tracks
    return album

album = make_album("Pink Floyd", "The Wall", 26)
print(album)

8-9.def show_message():
    print("I am learning Python.")

show_message()

8-10.def make_pizza(size, *toppings):
    print(f"Making a {size} pizza with the following toppings:")
    for topping in toppings:
        print(f"- {topping}")

make_pizza("large", "cheese", "pepperoni", "mushrooms")

8-11.def make_pizza(size, *toppings):
    print(f"Making a {size} pizza with the following toppings:")
    for topping in toppings:
        print(f"- {topping}")

make_pizza("small", "cheese", "olives")
make_pizza("medium", "tomatoes", "mushrooms", "bacon")

8-12 def build_profile(first_name, last_name, user_info):
    profile = {
        "first_name": first_name,
        "last_name": last_name,
    }
    for key, value in user_info.items():
        profile[key] = value
    return profile

user_profile = build_profile("Ankita", "patil", location="pune", occupation="Developer")
print(user_profile)

8-13.def print_three_times(message):
    for _ in range(3):
        print(message)

print_three_times("Hello, Python!")

8-14.def get_person_info(name, age, city):
    return f"{name} is {age} years old and lives in {city}."

person = get_person_info("Alice", 30, "New York")
print(person)

8-15.def divide_numbers(a, b):
    try:
        result = a / b
        print(f"The result is {result}")
    except ZeroDivisionError:
        print("You can't divide by zero!")

divide_numbers(10, 2)
divide_numbers(10, 0)

Chapter No.9
9-1. class Restaurant:
    def __init__(self, name, Menu_Category):
        self.name = name.title()
        self.Menu_Category = Menu_Category

    def describe_restaurant(self):
        msg = f"{self.name} serves wonderful {self.cuisine_type}."
        print(msg)

    def open_restaurant(self):4
        msg = f"{self.name} is open. Come on in!"
        print(msg)

restaurant = Restaurant('the mean queen', 'pizza')
print(restaurant.name)
print(restaurant.cuisine_type)
restaurant.describe_restaurant()
restaurant.open_restaurant()

9-2:
#Create three different instances from the Restaurant class, and call describe_restaurant() for each instance.​

mean_queen = Restaurant('the mean queen', 'pizza')
mean_queen.describe_restaurant()

ludvigs = Restaurant("ludvig's bistro", 'seafood')
ludvigs.describe_restaurant()

mango_thai = Restaurant('mango thai', 'thai food')
mango_thai.describe_restaurant()

9-3:
class User:
   
    def __init__(self, first_name, last_name, username, email, location):
        self.first_name = first_name.title()
        self.last_name = last_name.title()
        self.username = username
        self.email = email
        self.location = location.title()

    def describe_user(self):
        print(f"\n{self.first_name} {self.last_name}")
        print(f" Username: {self.username}")
        print(f" Email: {self.email}")
        print(f" Location: {self.location}")

    def greet_user(self):
        print(f"\nWelcome back, {self.username}!")

user1 = User('eric', 'matthes', 'e_matthes', 'e.matthes@example.com', 'alaska')
user1.describe_user()
user1.greet_user()

9-4:
class Restaurant:
    """A class representing a restaurant."""

    def __init__(self, name, cuisine_type):
        """Initialize the restaurant."""
        self.name = name.title()
        self.cuisine_type = cuisine_type
        self.number_served = 0

    def describe_restaurant(self):
        """Display a summary of the restaurant."""
        msg = f"{self.name} serves wonderful {self.cuisine_type}."
        print(msg)

    def open_restaurant(self):
        """Display a message that the restaurant is open."""
        msg = f"{self.name} is open. Come on in!"
        print(msg)

    def set_number_served(self, number):
        """Set the number of customers that have been served."""
        self.number_served = number

    def increment_number_served(self, additional_customers):
        """Increment the number of customers served."""
        self.number_served += additional_customers

restaurant = Restaurant('the mean queen', 'pizza')
print(f"Number served: {restaurant.number_served}")

restaurant.number_served = 23
print(f"Number served: {restaurant.number_served}")

restaurant.set_number_served(125)
print(f"Number served: {restaurant.number_served}")

restaurant.increment_number_served(200)
print(f"Number served: {restaurant.number_served}")

9-6: 
class IceCreamStand(Restaurant):
   
    def __init__(self, name, cuisine_type='ice_cream'):
        """Initialize attributes of the parent class."""
        super().__init__(name, cuisine_type)
        self.flavors = []

    def show_flavors(self):
        """Display the ice cream flavors available."""
        print("\nWe have the following flavors available:")
        for flavor in self.flavors:
            print(f"- {flavor.title()}")

ice_cream_stand = IceCreamStand('The Big Scoop')
ice_cream_stand.flavors = ['vanilla', 'chocolate', 'strawberry']
ice_cream_stand.describe_restaurant()
ice_cream_stand.show_flavors()

9-7.class User:
   
    def __init__(self, first_name, last_name, username, email, location):
        """Initialize the user."""
        self.first_name = first_name.title()
        self.last_name = last_name.title()
        self.username = username
        self.email = email
        self.location = location.title()

    def describe_user(self):
        """Display a summary of the user's information."""
        print(f"\n{self.first_name} {self.last_name}")
        print(f"  Username: {self.username}")
        print(f"  Email: {self.email}")
        print(f"  Location: {self.location}")

    def greet_user(self):
        """Display a personalized greeting to the user."""
        print(f"\nWelcome back, {self.username}!")

class Admin(User):
    """Represent an admin user."""

    def __init__(self, first_name, last_name, username, email, location):
        """Initialize attributes of the parent class."""
        super().__init__(first_name, last_name, username, email, location)
        self.privileges = ['can add post', 'can delete post', 'can ban user']

    def show_privileges(self):
        """Display the admin's privileges."""
        print("\nAdmin privileges:")
        for privilege in self.privileges:
            print(f"- {privilege}")

admin_user = Admin('eric', 'matthes', 'e_matthes', 'e_matthes@example.com', 'alaska')
admin_user.describe_user()
admin_user.show_privileges()
9-8: Privileges
class Privileges:
    """A class to store an admin's privileges."""

    def __init__(self, privileges=None):
        """Initialize the privileges."""
        if privileges is None:
            self.privileges = []
        else:
            self.privileges = privileges

    def show_privileges(self):
        """Display the privileges."""
        print("\nPrivileges:")
        if self.privileges:
            for privilege in self.privileges:
                print(f"- {privilege}")
        else:
            print("- This user has no privileges.")

class Admin(User):
    """Represent an admin user."""

    def __init__(self, first_name, last_name, username, email, location):
        """Initialize attributes of the parent class."""
        super().__init__(first_name, last_name, username, email, location)
        self.privileges = Privileges(['can add post', 'can delete post', 'can ban user'])

admin_user = Admin('eric', 'matthes', 'e_matthes', 'e_matthes@example.com', 'alaska')
admin_user.describe_user()
admin_user.privileges.show_privileges()
9-9: Battery Upgrade

class Battery:
    """A simple attempt to model a battery for an electric car."""

    def __init__(self, battery_size=75):
        """Initialize the battery's attributes."""
        self.battery_size = battery_size

    def describe_battery(self):
        """Print a statement describing the battery size."""
        print(f"This car has a {self.battery_size}-kWh battery.")

    def get_range(self):
        """Print a statement about the range this battery provides."""
        if self.battery_size == 75:
            range_km = 260
        elif self.battery_size == 100:
            range_km = 315

        print(f"This car can go approximately {range_km} miles on a full charge.")

    def upgrade_battery(self):
        """Upgrade the battery size."""
        if self.battery_size < 100:
            self.battery_size = 100
            print("Battery upgraded to 100 kWh.")
        else:
            print("Battery is already upgraded.")

class ElectricCar(Car):
    """Represent aspects of a car, specific to electric vehicles."""

    def __init__(self, make, model, year):
        """Initialize attributes of the parent class."""
        super().__init__(make, model, year)
        self.battery = Battery()

my_tesla = ElectricCar('tesla', 'model s', 2024)
my_tesla.battery.describe_battery()
my_tesla.battery.get_range()
my_tesla.battery.upgrade_battery()
my_tesla.battery.describe_battery()
my_tesla.battery.get_range()
 9-10: Imported Restaurant
class Restaurant:
    """A class representing a restaurant."""

    def __init__(self, name, cuisine_type):
        """Initialize the restaurant."""
        self.name = name.title()
        self.cuisine_type = cuisine_type

    def describe_restaurant(self):
        """Display a summary of the restaurant."""
        msg = f"{self.name} serves wonderful {self.cuisine_type}."
        print(f"\n{msg}")

    def open_restaurant(self):
        """Display a message that the restaurant is open."""
        msg = f"{self.name} is open. Come on in!"
        print(f"\n{msg}")
from restaurant import Restaurant

my_restaurant = Restaurant('the mean queen', 'pizza')
my_restaurant.describe_restaurant()
my_restaurant.open_restaurant()
9-11: Imported Admin
class User:
    """Represent a simple user profile."""

    def __init__(self, first_name, last_name, username, email, location):
        """Initialize the user."""
        self.first_name = first_name.title()
        self.last_name = last_name.title()
        self.username = username
        self.email = email
        self.location = location.title()

    def describe_user(self):
        """Display a summary of the user's information."""
        print(f"\n{self.first_name} {self.last_name}")
        print(f"  Username: {self.username}")
        print(f"  Email: {self.email}")
        print(f"  Location: {self.location}")

    def greet_user(self):
        """Display a personalized greeting to the user."""
        print(f"\nWelcome back, {self.username}!")

class Admin(User):
    """Represent an admin user."""

    def __init__(self, first_name, last_name, username, email, location):
        """Initialize attributes of the parent class."""
        super().__init__(first_name, last_name, username, email, location)
        self.privileges = ['can add post', 'can delete post', 'can ban user']

    def show_privileges(self):
        """Display the admin's privileges."""
        print("\nAdmin privileges:")
        for privilege in self.privileges:
            print(f"- {privilege}")
from user import Admin

admin_user = Admin('eric', 'matthes', 'e_matthes', 'e_matthes@example.com', 'alaska')
admin_user.describe_user()
admin_user.show_privileges()
9-12: Multiple Modules
class Restaurant:
    """A class representing a restaurant."""

    def __init__(self, name, cuisine_type):
        """Initialize the restaurant."""
        self.name = name.title()
        self.cuisine_type = cuisine_type

    def describe_restaurant(self):
        """Display a summary of the restaurant."""
        msg = f"{self.name} serves wonderful {self.cuisine_type}."
        print(f"\n{msg}")

    def open_restaurant(self):
        """Display a message that the restaurant is open."""
        msg = f
        ::contentReference[oaicite:22]{index=22}




















